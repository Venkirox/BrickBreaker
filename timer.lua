
-- Timer = {}                           No idea how to make this work
-- Timer.__index = Timer
-- --setmetatable(childmeta, Timer)

-- function Timer.new(settings)
--     local instance = setmetatable({}, Timer)
--     --local instance = setmetatable(Timer.new(settings), childmeta)
--     instance.time = settings.time or 10
--     return instance
-- end

-- function Timer:update(dt)
    
    
    
-- end

-- function Timer:draw()
    
-- end

-- return Timer



-- Timers = {activeTimers = {}}                     Timer second file
-- local newTimer = require("timer")

-- function Timers:create(timer)
--     print('x')

--     table.insert(self.activeTimers, newTimer.new({time = timer}))
    
-- end

